请根据以下 schema 生成 {count} 条不同的 JSON 数据:

字段要求:
{field_descriptions}

要求：
1. 生成的每条 JSON 必须严格符合 schema 定义，字段名必须与 schema 完全一致
2. 数据要合理、真实，避免使用占位符或测试数据
3. 邮箱必须是真实可用的格式，如 user.name@example.com，不能是 test@test.com 这样的占位符
4. 对于有模式要求的字段（如 ID、VIN 等），必须严格按照指定格式生成
5. 生成的数据要具有多样性，避免重复
6. 只返回 JSON 数组，不要添加任何其他文本
7. 确保所有必填字段都有值
8. 对于日期和时间字段，生成合理的过去或当前日期，避免使用未来日期
9. 对于数值字段，生成在合理范围内的值，避免极端值
10. 对于枚举字段，随机选择不同的枚举值，确保数据多样性
11. 对于字符串字段，生成有意义的内容，避免无意义的随机字符
12. 对于布尔字段，随机生成 true 或 false 值，确保数据多样性
13. 重要：每条数据的 id 字段必须是不同的值，建议使用递增的数字（如 1,2,3...）或随机数

示例（单条数据格式）:
```json
{example}
```

请直接返回 JSON 数组格式，例如：[{...}, {...}, ...]
