请根据以下 schema 生成一条 JSON 数据:

字段要求:
{field_descriptions}

生成规则：
1. 生成的 JSON 必须严格符合 schema 定义，字段名必须与 schema 完全一致
2. 数据要合理、真实，避免使用占位符或测试数据
3. 邮箱必须是真实可用的格式，如 user.name@example.com
4. 对于 ID 字段，必须使用指定的序号值（从 1 开始递增）
5. 对于枚举字段，按顺序选择枚举值，确保覆盖所有枚举选项
6. 只返回 JSON 数据，不要添加任何其他文本
7. 确保所有必填字段都有值
8. 对于日期和时间字段，生成合理的日期
9. 对于数值字段，生成在合理范围内的值
10. 对于字符串字段，生成有意义的内容
11. 对于布尔字段，按顺序交替生成 true 或 false

当前生成序号: {sequence}

示例：
```json
{example}
```
